_target_: models.lno.LFNO

name: lno
modes: 32
hidden_channels: 64
x_dim: 2
coord_channels: 0

# --- Spatial & Local Configs ---
disco_layers: true
disco_kernel_shape: [5, 5]
default_in_shape: [128, 128]  # Updated to match actual dataset resolution

n_layers: 4                   # Number of LocalNO blocks for FiLM injection