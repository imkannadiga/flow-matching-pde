defaults:
  - _self_
  - train: debug
  - data: navier_stokes
  - model: fno
  - eval: base

experiment_name: trial

hydra:
  run:
    dir: evaluation_runs/${model.name}-${data.name}-${experiment_name}
