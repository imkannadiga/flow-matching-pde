_target_: models.fno.FNO

name: fno
modes: 64
vis_channels: 1
hidden_channels: 64
proj_channels: 64
x_dim: 2
t_scaling: 1000
# Avoids dense complex einsum (cublasCgemmStridedBatched) seen on some CUDA/PyTorch stacks.
separable: true
