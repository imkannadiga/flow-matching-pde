_target_: models.lno.LNO

modes: 16
visch: 1
hch: 64
xdim: 2
t_scaling: 1000
disco_kernel_shape: [5, 5]

default_in_shape: [64, 64]

input_dim: 2
hidden_dim: 64

lengthscale: 0.001
var: 1.0
sigma_min: 0.0001

device: "cuda"
