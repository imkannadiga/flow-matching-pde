# @package _global_
# Multirun: ``python train.py --config-path configs --config-name experiment/phase1 --multirun``
defaults:
  - _self_
  - /train

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      paradigm: fm,ar,diffusion
      seed: 0,1,2
