_target_: models.amfno.AMFNO

name: amfno
modes: 32
vis_channels: 1
hidden_channels: 64
proj_channels: 64
x_dim: 2
t_scaling: 1
coord_channels: 0
film_param_dim: ${conditioning.param_dim}
param_dim: 2
context_dim: 8
separable: true
